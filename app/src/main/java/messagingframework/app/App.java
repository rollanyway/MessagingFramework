/*
 * This Java source file was generated by the Gradle 'init' task.
 */
package messagingframework.app;

import messagingframework.list.LinkedList;

import static messagingframework.utilities.StringUtils.join;
import static messagingframework.utilities.StringUtils.split;
import static messagingframework.app.MessageUtils.getMessage;

import org.apache.commons.text.WordUtils;

public class App {
    public static void main(String[] args) {
        LinkedList tokens;
        tokens = split(getMessage());
        String result = join(tokens);
        System.out.println(WordUtils.capitalize(result));
    }
}
